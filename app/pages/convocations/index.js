import Page from 'classes/page'

export default class Convocations extends Page {
  constructor() {
    super({
      id: 'convocations',
      element: '#conv',
    })
  }

  create() {
    super.create()
  }
}
